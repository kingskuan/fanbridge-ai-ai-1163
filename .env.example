# FanBridge AI runs fully client-side with no required secrets.
#
# Optional — override the default X Layer network selection:
# NEXT_PUBLIC_DEFAULT_NETWORK=mainnet   # or "testnet"
#
# Optional — if you later wire lib/agent.ts to a real LLM backend, keep the
# key server-side (do NOT prefix with NEXT_PUBLIC):
# ANTHROPIC_API_KEY=sk-ant-...
